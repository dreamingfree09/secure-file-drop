run:
  timeout: 2m
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gofmt
    - goimports
    - deadcode
    - unused
issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
